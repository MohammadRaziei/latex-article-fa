% =====================================================================
% تمپلیت مقاله IEEE (IEEEtran) به زبان فارسی با XeLaTeX و xepersian
% =====================================================================
%
% نحوه‌ی کامپایل (این نسخه با BibTeX است):
%   باید حتماً با XeLaTeX کامپایل شود (نه pdflatex):
%       xelatex main.tex
%       bibtex main
%       xelatex main.tex
%       xelatex main.tex
%
% سوئیچ بین یک‌ستونی و دوستونی:
%   کافیست در خط \documentclass زیر، کلمه‌ی "onecolumn" را
%   اضافه یا حذف کنید. فرمت‌بندی به‌هم نمی‌ریزد چون بقیه‌ی فایل
%   با هر دو حالت سازگار نوشته شده است.
%
%     دوستونی (پیش‌فرض IEEE):  \documentclass[journal]{IEEEtran}
%     یک‌ستونی:                \documentclass[journal,onecolumn]{IEEEtran}
%
% =====================================================================

\documentclass[journal]{IEEEtran}
% برای یک‌ستونی شدن، خط بالا را با خط زیر جایگزین کنید:
% \documentclass[journal,onecolumn]{IEEEtran}

% ------------------------- بسته‌های پایه -------------------------
\usepackage{amsmath,amssymb,amsfonts}
\usepackage{graphicx}
\usepackage{cite}
\usepackage{booktabs}
\usepackage{xcolor}
\usepackage{setspace}
\usepackage{cuted}
%\usepackage{abstract}

% ------------------------- پشتیبانی فارسی -------------------------
% باید بعد از بسته‌های بالا لود شود.
% توجه: عمداً hyperref را اینجا دستی لود نکرده‌ایم؛ بسته‌ی xepersian
% (از طریق bidi) خودش hyperref را با تنظیمات درست برای راست‌به‌چپ
% لود می‌کند. لود دستی hyperref قبل از xepersian باعث خطای
% «TeX capacity exceeded, sorry [input stack size=10000]» می‌شود.
\usepackage{xepersian}

% فونت‌های متن (در صورت نیاز فونت سیستم خود را جایگزین کنید)
\settextfont{XB Niloofar}
\setlatintextfont{TeX Gyre Termes}
\setdigitfont{XB Niloofar}

\linespread{1.2}
%\textlabeldelim{:}

\makeatletter
\renewcommand{\IEEEkeywordsname}{کلیدواژه‌ها}
\def\abstract{\@IEEEabskeysecsize\bfseries\textit{\abstractname}:\relax}
\def\IEEEkeywords{\@IEEEabskeysecsize\bfseries\textit{\IEEEkeywordsname}:\relax}
\makeatother





% ------------------------- اطلاعات مقاله -------------------------
\title{عنوان مقاله در اینجا نوشته شود}

\author{
	نام نویسنده اول$^{1}$،~
	نام نویسنده دوم$^{2}$،~
	نام نویسنده سوم$^{3}$
	\thanks{$^{1}$نویسنده اول با دانشکده/دانشگاه مربوطه در ارتباط است. (ایمیل: \lr{author1@example.com})}
	\thanks{$^{2}$نویسنده دوم با دانشکده/دانشگاه مربوطه در ارتباط است.}
	\thanks{$^{3}$نویسنده سوم با دانشکده/دانشگاه مربوطه در ارتباط است.}
}

% =====================================================================
\begin{document}
	
	\maketitle
	
	\begin{strip}
		\begin{abstract}
			در این قسمت چکیده‌ی مقاله نوشته می‌شود. چکیده باید کوتاه و گویا باشد و
			هدف، روش، نتایج و دستاورد اصلی پژوهش را در یک یا دو پاراگراف کوتاه بیان کند.
			معمولاً طول چکیده بین ۱۵۰ تا ۲۵۰ کلمه پیشنهاد می‌شود.
		\end{abstract}
		
		\begin{IEEEkeywords}
			کلیدواژه اول, کلیدواژه دوم, کلیدواژه سوم, کلیدواژه چهارم
		\end{IEEEkeywords}
	\end{strip}
	% =====================================================================
	\section{مقدمه}
	% توجه: از \IEEEPARstart (حرف بزرگ آغازین لاتین) در متن فارسی استفاده
	% نشده، چون این دستور برای الفبای لاتین طراحی شده و با حروف فارسی
	% به‌درستی کار نمی‌کند و باعث وارنینگ و کاراکتر گم‌شده می‌شود.
	متن مقدمه‌ی مقاله در این بخش نوشته می‌شود. در مقدمه معمولاً
	اهمیت موضوع، مرور کوتاهی بر پیشینه‌ی پژوهش، مسئله‌ی مورد بررسی، و در انتها
	ساختار کلی مقاله بیان می‌شود.
	
	می‌توانید به مراجع مانند \cite{ref1} یا \cite{ref2} ارجاع دهید.
	
	% =====================================================================
	\section{کارهای مرتبط}
	در این بخش پژوهش‌های پیشین مرتبط با موضوع مقاله بررسی و مقایسه می‌شوند.
	
	% =====================================================================
	\section{روش پیشنهادی}
	در این بخش روش، مدل یا الگوریتم پیشنهادی به‌طور کامل شرح داده می‌شود.
	
	\subsection{زیربخش اول}
	متن زیربخش اول.
	
	\subsection{زیربخش دوم}
	متن زیربخش دوم.
	
	% نمونه‌ی یک فرمول ریاضی
	\begin{equation}
		y = \alpha x + \beta
		\label{eq:sample}
	\end{equation}
	
	همان‌طور که در رابطه‌ی~\eqref{eq:sample} مشاهده می‌شود...
	
	% نمونه‌ی یک جدول
	\begin{table}[!t]
		\caption{عنوان جدول نمونه}
		\label{tab:sample}
		\centering
		\begin{tabular}{ccc}
			\toprule
			ستون اول & ستون دوم & ستون سوم \\
			\midrule
			مقدار ۱ & مقدار ۲ & مقدار ۳ \\
			مقدار ۴ & مقدار ۵ & مقدار ۶ \\
			\bottomrule
		\end{tabular}
	\end{table}
	
	% نمونه‌ی درج شکل (فایل تصویر را در کنار main.tex قرار دهید)
	% \begin{figure}[!t]
		% \centering
		% \includegraphics[width=\linewidth]{figure1.png}
		% \caption{عنوان شکل نمونه}
		% \label{fig:sample}
		% \end{figure}
	
	% =====================================================================
	\section{نتایج و بحث}
	در این بخش نتایج آزمایش‌ها یا شبیه‌سازی‌ها ارائه و تحلیل می‌شود.
	
	% =====================================================================
	\section{نتیجه‌گیری}
	جمع‌بندی نهایی مقاله و پیشنهادهایی برای کارهای آینده در این بخش بیان می‌شود.
	
	% =====================================================================
	% منابع با BibTeX (فایل references.bib را در همین پوشه ببینید)
	\bibliographystyle{IEEEtran}
	\bibliography{references}
	
\end{document}
